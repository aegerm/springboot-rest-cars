spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/db_cars
    username: root
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: 'create-drop'
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDB10Dialect